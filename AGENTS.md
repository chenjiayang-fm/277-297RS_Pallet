# AGENTS.md

## 基本原则

本项目为嵌入式 C/C++ 应用层项目。

修改代码时，优先遵循项目现有代码的写法、结构和命名方式，
不要为了所谓的“最佳实践”大规模重构已有代码。

目标是：
- 改动尽可能小
- 逻辑尽可能简单
- 与现有应用层代码风格保持一致
- 不引入没有必要的抽象和复杂度
- 优先解决实际问题，不做无关优化


## 代码风格

1. 修改或新增代码前，先阅读当前文件以及附近相关模块的代码，
   了解现有代码风格后再实现。

2. 新代码应尽量保持与现有应用层代码一致，包括：
   - 函数命名
   - 变量命名
   - 缩进和括号风格
   - 日志格式
   - 错误处理方式
   - 函数组织方式
   - 注释风格

3. 不要因为个人偏好修改已有代码风格。

4. 如果存在多种实现方式，优先选择：
   - 代码量更少
   - 逻辑更直观
   - 更容易维护
   - 更符合当前项目已有写法
   的方案。


## 简化代码

尽量避免过度设计。

不要无必要地增加：
- wrapper
- helper function
- class
- abstraction layer
- callback
- 状态机
- 临时结构体
- 多层函数调用

一个简单 if 能解决的问题，不要设计复杂框架。

不要添加没有实际作用的防御性判断。

例如，如果根据当前调用关系某个指针一定非 NULL，
并且项目原有代码也没有进行 NULL 判断，
不要机械地增加：

if (ptr == NULL) {
    return -1;
}

只有存在真实风险、外部输入或调用路径不能保证时，
才增加必要的参数检查。


## 控制修改范围

严格限制修改范围。

如果任务只需要修改一个函数，就不要顺便重构整个文件。

如果任务只需要修改一个模块，就不要修改无关模块。

除非用户明确要求，否则不要：
- 重命名已有函数
- 重命名已有变量
- 调整大量代码格式
- 重构正常工作的代码
- 修改无关逻辑
- 增加新的依赖


## 测试规则

可以使用测试、编译、静态检查或临时测试代码验证修改是否正确。

但是：

**不要在项目中新增正式 test 文件或测试框架，除非用户明确要求。**

允许：
- 编译验证
- 运行已有测试
- 使用已有测试程序
- 临时编写测试代码进行验证
- 使用日志验证
- smoke test
- 静态分析

如果为了验证临时创建了测试文件，
验证完成后应删除，不要提交到项目。

优先执行与本次修改直接相关的最小测试，
不要为了一个小修改运行大量无关测试。


## 修改前

在修改代码前：

1. 先查看相关代码。
2. 找出项目当前实现方式。
3. 尽量复用已有函数和逻辑。
4. 确定最小修改方案。
5. 再开始修改。


## 修改后

修改完成后：

1. 检查是否存在明显编译错误。
2. 尽可能执行相关编译或验证。
3. 检查是否引入不必要的复杂代码。
4. 检查是否存在重复逻辑。
5. 检查是否修改了任务范围之外的代码。

如果有更简单的实现，应优先使用更简单的实现。


## 与用户沟通

不要长篇解释简单修改。

完成后简要说明：
- 修改了什么
- 修改了哪些文件
- 是否进行了编译/测试
- 是否存在需要用户注意的问题

如果发现可能需要大规模重构，
不要直接重构，先说明原因并询问用户。